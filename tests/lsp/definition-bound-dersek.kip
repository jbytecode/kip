(* Test go-to-definition for bound variables in dersek expressions *)

(bu tam-sayıyla) (şu tam-sayının) toplamı,
  (bunla şunun toplamına) sonuç dersek,
  sonucun 2'ye bölümüdür.

(3'le 5'in toplamını) yaz.
